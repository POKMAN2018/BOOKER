<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>MY BOOK</title>
	<%- include("bootrap") %>
</head>
<body class="bg-dark text-light">


<center>
<% if (!doc) { %>
  <p class="display-5 text-light">ไม่มีหนังสือที่เป็นของคุณ</p>
<% } %>



<table class="table bg-light w-50 mt-5">
  <thead>
    <tr>
      <th scope="col">ชื่อหนังสือ</th>
      <th scope="col">ดูหนังสือ</th>
      <th scope="col">การเข้าชม</th>
      <th scope="col">ให้ยืมเมื่อ</th>
      <th scope="col">ลบ</th>
    </tr>
  </thead>
  <tbody>
    <% Object.values(doc).forEach((docer)=> { %>
    <tr>
      <th><%= docer.title %></th>
      <td><a href="/view_book/<%= docer._id %>" class="btn btn-primary">ดูหนังสือ</a></td>
      <td><%= docer.view %></td>
      <td><%= docer.date %></td>
      <td><a href="/del_my_book/<%= docer._id %>" class="btn btn-danger">ลบ</a></td>
    </tr>
      <% }) %>
  </tbody>
</table>


</center>


	


</body>
</html>